The startup period for all tests is 120 seconds.  For tests with more than two maps, that generally means that all users were running through the same set of maps at the same time (though for different models).